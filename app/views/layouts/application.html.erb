<!DOCTYPE html>
<html>
  <head>
    <style>
      * {
        text-align: center;
        font-family: Georgia, serif;
        color: #000000;
      }
      nav {
        list-style-type: none;
        margin: 0;
        padding: 0;
        overflow: hidden;
        background-color: #000000;
      }
      dt {
        float: left;
      }
      dt a {
        display: block;
        color: white;
        text-align: center;
        padding: 7px 8px;
        text-decoration: none;
      }
      dt a:hover {
        background-color: #8B0000;
      }
      ul {
	      list-style: none;
      }
    </style>
    <title>Agent Agenda: Mission Possible</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
   <body>
  <br>
    <div>
    <nav>
      <ul>
        <% if logged_in? %>
        <dt><%= link_to "New Mission", new_mission_path %></dt>
        <dt><%= link_to "Add Mission Feedback", new_feedback_path %></dt>
        <dt><%= link_to "All Missions", missions_path %></dt>
        <dt><%= link_to "My Missions", user_missions_path(current_user) %></dt>
        <dt><%= link_to "Most Active Users", users_most_active_path %></dt>
        <dt><%= link_to "Home", root_path%></dt>
        <dt style="float:right"><%= button_to "Logout", logout_path, method:"delete" %></dt>
        <% else %>
        <dt><%= link_to "Sign Up", signup_path %></dt>
        <dt><%= link_to "Log In", login_path %></dt>
        <dt><%= button_to "Log In With Google", '/auth/google_oauth2' %></dt>
        <% end %>
      </ul>
      </nav>
    </div>

    <div>
      <% if flash[:message] %>
        <%= flash[:message] %>
      <% end %>
      <%= @error %>
    </div>

    <%= yield %>
    <div class="wrapper">
      <footer class="branding">
        <small>&copy; 2021 <strong>Agent Agenda</strong></small>
      </footer>
    </div>

  </body>
</html>